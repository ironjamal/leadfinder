# Google Maps Platform API key with the "Places API (New)" enabled.
# Keep this secret — it is only read on the server (never sent to the browser).
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here
